% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build.R
\name{quartofix}
\alias{quartofix}
\title{Patch a pkgdown site's Quarto vignettes by restoring}
\usage{
quartofix(dst_path = "docs", pkg_path = ".", pattern = "[.]html$")
}
\arguments{
\item{dst_path}{Path to the built pkgdown site (\verb{docs/} by default).}

\item{pkg_path}{Path to the package root, used to locate the \code{.qmd}
source of each article (searched under \verb{vignettes/}) so that
\code{\link[=fix_quarto_callouts]{fix_quarto_callouts()}} can recover callout types lost to custom
titles. Set to \code{NULL} to skip source lookup entirely.}

\item{pattern}{Regex selecting which HTML files to inspect. Defaults to
every \code{.html} file, which is harmless but slower on large sites; narrow
it (e.g. \code{"^articles/"}) if you know where your Quarto articles land.}
}
\value{
Invisibly, the paths of the HTML files that were modified.
}
\description{
Walks the HTML files pkgdown generated from \code{.qmd} articles, restores
callout markup with \code{\link[=fix_quarto_callouts]{fix_quarto_callouts()}}, undoes pkgdown's corruption
of Quarto tabsets with \code{\link[=fix_quarto_tabsets]{fix_quarto_tabsets()}}, detects code annotations
with \code{\link[=fix_quarto_annotations]{fix_quarto_annotations()}}, and - for any file that contains a
tabset or code annotation - links in the small
\code{quartofix.css}/\code{quartofix.js} assets that restore their styling and
interactivity (copied once into \verb{<dst_path>/quartofix/}).
}
\details{
Run this \emph{after} \code{pkgdown::build_site()} (or \code{build_articles()}), since it
operates on the rendered HTML, not the \code{.qmd} source.
}
