% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotations.R
\name{fix_quarto_annotations}
\alias{fix_quarto_annotations}
\title{Detect Quarto code annotations in a pkgdown-rendered HTML file}
\usage{
fix_quarto_annotations(html_path)
}
\arguments{
\item{html_path}{Path to an HTML file to inspect.}
}
\value{
The number of code annotations found, invisibly.
}
\description{
Unlike callouts and tabsets, Quarto's code-annotation markup (the
\code{code-annotation-anchor} links and their \code{dl.code-annotation-container-grid}
targets) survives pkgdown's \code{minimal: TRUE} render intact - it's only the
interactivity (click-to-highlight, hover) that's lost, because that theme
strips the Bootstrap/Quarto JS the annotations depend on. So there's no
markup to repair here, unlike \code{\link[=fix_quarto_callouts]{fix_quarto_callouts()}} and
\code{\link[=fix_quarto_tabsets]{fix_quarto_tabsets()}}; this function just counts the annotations present,
which \code{\link[=quartofix]{quartofix()}} uses to decide whether to link in the JS/CSS that
restores their behavior.
}
