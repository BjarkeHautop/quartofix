% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tabsets.R
\name{fix_quarto_tabsets}
\alias{fix_quarto_tabsets}
\title{Undo pkgdown's corruption of Quarto tabsets}
\usage{
fix_quarto_tabsets(html_path)
}
\arguments{
\item{html_path}{Path to an HTML file to rewrite in place.}
}
\value{
The number of tabsets repaired, invisibly.
}
\description{
pkgdown's own \code{tweak_tabsets()} (written for R Markdown's
\verb{## Heading \{.tabset\}} syntax) runs on every \verb{<div>} whose class merely
\emph{contains} the substring "tabset" - which also matches Quarto's
\code{.panel-tabset} divs, even though their markup shape is completely
different. Quarto's minimal-mode render produces:
}
\details{
\if{html}{\out{<div class="sourceCode html">}}\preformatted{<div class="panel-tabset">
  <ul class="panel-tabset-tabby">...</ul>
  <div class="tab-content">
    <div id="tabset-1-1">...</div>
    <div id="tabset-1-2">...</div>
  </div>
</div>
}\if{html}{\out{</div>}}

\code{tweak_tabset()} looks for \emph{direct \verb{<div>} children} of \code{.panel-tabset} to
use as "tabs" - it finds exactly one (the \code{.tab-content} wrapper), and
mistakes it for a single R Markdown tab: the first tab's content is
unwrapped into the new nav \verb{<button>} (as its "title"), and the remaining
tabs are left, still correctly \code{id}-ed, nested inside one bogus
\code{id="NA"} pane. The original \verb{<ul class="panel-tabset-tabby">} is never
touched, so it survives as ground truth for tab order and titles.

This function reverses that: it uses the untouched \code{panel-tabset-tabby}
list to learn how many tabs there should be, pulls tab 1's content back
out of the corrupted nav button, reclaims tabs 2..N from inside the
bogus pane (they're already intact), and rebuilds a clean
\code{.tab-content} div - the shape \code{\link[=quartofix]{quartofix()}}'s CSS/JS expect.
}
