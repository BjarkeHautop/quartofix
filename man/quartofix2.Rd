% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build2.R
\name{quartofix2}
\alias{quartofix2}
\title{Patch a pkgdown site by splicing genuine Quarto HTML}
\usage{
quartofix2(dst_path = "docs", pkg_path = ".", pattern = "[.]html$")
}
\arguments{
\item{dst_path}{Path to the built pkgdown site (\verb{docs/} by default).}

\item{pkg_path}{Path to the package root, used to locate the \code{.qmd}
source of each article (searched under \verb{vignettes/}) so that
\code{\link[=fix_quarto_callouts]{fix_quarto_callouts()}} can recover callout types lost to custom
titles. Set to \code{NULL} to skip source lookup entirely.}

\item{pattern}{Regex selecting which HTML files to inspect. Defaults to
every \code{.html} file, which is harmless but slower on large sites; narrow
it (e.g. \code{"^articles/"}) if you know where your Quarto articles land.}
}
\value{
Invisibly, the paths of the HTML files that were modified.
}
\description{
An alternative to \code{\link[=quartofix]{quartofix()}}. Instead of reconstructing callout and
tabset markup from what pkgdown left behind, this re-renders each
article's \code{.qmd} source with \code{quarto::quarto_render()} and splices the
genuine rendered nodes into pkgdown's HTML in place of the degraded ones.
This is more faithful to Quarto's actual output (no callout-type
guessing needed) but costs a second, full Quarto render per article,
which can be slow for expensive vignettes. Kept separate from
\code{\link[=quartofix]{quartofix()}} while that tradeoff is evaluated.
}
\details{
Only articles with a discoverable \code{.qmd} source (see \code{pkg_path}) are
spliced; articles without one are left untouched. Code annotations are
not yet spliced by this function - they're handled the same way as
\code{\link[=quartofix]{quartofix()}} (asset injection only).
}
