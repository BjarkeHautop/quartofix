% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/callouts.R
\name{fix_quarto_callouts}
\alias{fix_quarto_callouts}
\title{Restore Quarto callout markup in a pkgdown-rendered HTML file}
\usage{
fix_quarto_callouts(html_path, qmd_path = NULL)
}
\arguments{
\item{html_path}{Path to an HTML file to rewrite in place.}

\item{qmd_path}{Optional path to the \code{.qmd} source that produced
\code{html_path}, used to recover callout types lost to custom titles.}
}
\value{
The number of callouts rewritten, invisibly.
}
\description{
pkgdown renders \code{.qmd} articles with \code{theme: "none"} and \code{minimal: TRUE}
(see \code{pkgdown:::quarto_format()}), which strips the CSS Quarto callouts
rely on. Under those settings, Quarto's callout Lua filter falls back to a
plain, class-less blockquote:
}
\details{
\if{html}{\out{<div class="sourceCode html">}}\preformatted{<div>
<blockquote>
<p><strong>Note</strong></p>
<p>Hello note</p>
</blockquote>
</div>
}\if{html}{\out{</div>}}

This function finds that pattern - an attribute-less \verb{<div>} whose only
child is a \verb{<blockquote>} whose first child is a \verb{<p>} containing nothing
but a \verb{<strong>} - and rewrites it into Quarto's native callout markup
(\verb{div.callout.callout-style-default.callout-<type>}), so it can be styled
by the CSS bundled with this package (see \code{\link[=quartofix]{quartofix()}}).

A hand-written blockquote is never wrapped in a bare \verb{<div>} by Pandoc, so
this signature does not collide with genuine author content.

If a callout used a custom title (\verb{## My Title} instead of the default),
pkgdown's render loses the callout type along with the title text, so the
type can't be read back from the HTML alone. Pass \code{qmd_path} (the
original \code{.qmd} source, which pkgdown always ships alongside the built
HTML) and this function recovers the type by reading the \verb{::: \{.callout-*\}}
fences in document order and matching them positionally against the
degraded callouts - this works regardless of custom titles. It's used only
as a fallback when the count of callouts in the source doesn't match the
count found in the HTML (which would mean a mismatch is possible), the
title text is used to guess the type instead.
}
